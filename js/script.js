const element = document.getElementById("element");

var typed = new Typed(element, {
    strings: ['Designer', 'Developer'],
    typeSpeed: 45,
    backSpeed: 80,
    backDelay: 500,
    startDelay: 1000,
    loop: true,
});